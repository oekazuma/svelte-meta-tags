---
'svelte-meta-tags': minor
---

feat: add `openGraph.image` as a shortcut alias for a single image. When both `image` and `images` are provided, `image` is rendered as the first `og:image`, followed by `images`.
